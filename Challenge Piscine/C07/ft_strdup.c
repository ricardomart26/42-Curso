#include <stdlib.h>

int ft_strlen(char *src)
{
  int c;

  while (*src)
    c++;

  return (c);
}

char ft_strcpy(char *dest, char *src)
{
}

char *ft_strdup(char *src)
{
  int c;
  char *duplicado;

  duplicado = (char)
      duplicado = ft_strcpy(duplicado, src);
}